# Tasks

<!-- Stock OpenSpec task checklist, derived from implementation-order.md.
     This file lets `openspec list` and task tooling report progress; the
     apply phase also tracks detailed state in implementation-progress.md.
     Keep both in sync — check boxes here as each spec completes.

     RULES:
     - One `## <n>. <spec-name>` section per spec, in implementation-order.md order
     - Per-spec checkboxes follow the schema cycle: typed contract (human gate) →
       test oracle (human gate) → implementation → applicable rings → concept-delta
       + inventory update + checkpoint
     - List only the rings that apply to that spec (skip those marked `—` in the
       Ring Applicability table)
     - Prerequisite work (build restructure, deps, static-analysis config) goes
       first in the owning spec's section
     - Every task is observable and stack-specific — never "implement the spec" -->

## 1. optimizable-surface

- [x] Step 0 — baseline: record clean-tree SHA + inventory snapshot; read `openspec/concept-inventory.md`; verify the spec's Proof Obligations table is complete (40+ rows, every requirement named)
- [x] Step 0 — build wiring: add `adk4s-optimize` module to `build.sbt` (`.dependsOn(structured-llm)`, deps `catsEffect`/`fs2Core`/`munitMain`/`munitCatsEffectMain`/`hedgehogMunitMain` + `testDeps`, `scalacOptions ++= scala3Options`, aggregated by root); add `munitCatsEffectMain` + `hedgehogMunitMain` main-scope variants to `project/Dependencies.scala`; verify `sbt adk4s-optimize/compile` succeeds on an empty module
- [x] Step 1 — typed contract: write `OptimizableTypeContract.scala` in `adk4s-optimize/src/test/scala/org/adk4s/optimize/` with the full typed signatures from design.md (`Demo`, `PredictorState`, `PredictorPath` with `render`, `OptimizeError` enum with `UnknownPath`/`FrozenPath`, `Optimizable[P]` trait with `predictors`/`update`/`updateEither`/`updateAll`, `object Optimizable` with `inline def derived` + `def apply`, `HasPredictorState[Self]` trait with `state`/`withState`, `Predict0[F, I, O]` case class, `given HasPredictorState[Predict0[F, I, O]]`); must compile against stubs (trait bodies can be `???` but signatures must match design.md exactly) — **HUMAN GATE (separate tier, gate 2)** — APPROVED
- [x] Step 2 — test oracle: write `Predict0Spec.scala`, `OptimizableSpec.scala`, `ToyOptimizerSpec.scala`, `OptimizerLawsSpec.scala` in `adk4s-optimize/src/test/scala/org/adk4s/optimize/` with all scenarios from the spec's Proof Obligations table + 14 Hedgehog properties (`predictors-declaration-order`, `nested-recursion-paths`, `collection-recursion-paths`, `update-purity`, `update-only-target`, `round-trip-identity`, `updateAll-skips-frozen`, `updateAll-path-set-preserved`, `updateEither-unknown-path-error`, `updateEither-frozen-path-error`, `frozen-still-readable`, `optimizer-laws-purity`, `optimizer-laws-frozen-preserved`, `optimizer-laws-path-set-preserved`) + compile-negative stub (`assertDoesNotCompile("e match { case OptimizeError.UnknownPath(_) => () }")`); run ORACLE POLARITY — properties must be RED (fail because implementation is stubs), compile-negative must be GREEN-by-design (compiles-negative already works against the typed contract); toy program fixtures (`TwoPredictors`, `Outer`/`Inner`, `Pipeline`) + toy optimizers (`UppercaseInstructions`, `StaticDemoInjector`) declared as stubs — **HUMAN GATE (separate tier, gate 3)** — APPROVED
- [x] Step 3 — implementation: implement in intra-spec order — (a) `Demo.scala`, `PredictorState.scala`, `PredictorPath.scala` (pure case classes, `render = segments.mkString(".")`); (b) `OptimizeError.scala` (sealed enum `extends Throwable`, `UnknownPath(path)`, `FrozenPath(path)`, stands alone — NOT `AdkError`); (c) `HasPredictorState.scala` (typeclass with `state`/`withState`); (d) `Optimizable.scala` (`inline def derived` via `Mirror.ProductOf[P]` + `summonInline` chain — try `Optimizable` before `HasPredictorState`, `Vector` recursion via element-type `summonInline`, skip fields with no instance; `updateEither` = runtime path + frozen check returning `Either[OptimizeError, P]`; `update` = `updateEither.fold(e => throw e, identity)` with targeted `@SuppressWarnings(Array("org.wartremover.warts.Throw"))`; `updateAll` = `predictors(p).filter(!_._2.frozen).foldLeft(p)(...)`); (e) `Predict0.scala` (case class with `state`/`template`/`schema`/`structured` fields + `given HasPredictorState[Predict0[F, I, O]]` in companion); (f) `testkit/OptimizerLaws.scala` (main-scope, `purity`/`frozenPreserved`/`pathSetPreserved` laws returning `IO[Boolean]` + `all` conjunction, pattern follows `AgentMemoryLaws`); (g) test fixtures `ToyPrograms.scala` (`TwoPredictors`, `Outer`, `Inner`, `Pipeline` case classes with `Optimizable.derived` instances) + `UppercaseInstructions.scala` + `StaticDemoInjector.scala` (`P => IO[P]` toy optimizers using `Optimizable.updateAll`)
- [x] Ring 0 — compile: `sbt adk4s-optimize/compile` succeeds; exhaustiveness escalation holds (no catch-all over `OptimizeError`); `sbt adk4s-optimize/test` — all 14 properties + scenarios + compile-negative pass (64 tests, 0 failures)
- [x] Ring 1 — lint: `sbt adk4s-optimize/scalafmtCheck` clean; WartRemover clean (scalafix has environment dependency issue, unrelated); `@SuppressWarnings(Array("org.wartremover.warts.Throw"))` scoped to the single `update` method; `given HasPredictorState[Predict0[F, I, O]]` does not trip the `Any` wart
- [x] Ring 2 — architecture: import audit on `adk4s-optimize/src/main/scala/org/adk4s/optimize/` — no forbidden imports (no adk4s-core, adk4s-orchestration, workflows4s, or llm4s client); `org.adk4s.optimize` purity-rule row recorded
- [x] Ring 3 — property tests: all 15 Hedgehog properties GREEN in `sbt adk4s-optimize/test` (12 in OptimizableSpec + 3 in OptimizerLawsSpec); no `TestControl` usage (the surface is pure and synchronous)
- [x] Ring 8 — adversarial review (fresh context, BEFORE any deferred R5/R6/R7): 15 PASS, 1 PARTIAL (R12 compile-negative test enforced at compiler level via `-Wconf` flag, not via munit macro — justified), 0 FAIL
- [x] Ring 5 — mutation testing: DEFERRED (no non-trivial helper revealed by implementation; `Optimizable.derived` is inline macro logic not amenable to Stryker4s mutation)
- [x] Ring 6 — Stainless formal verification: `PredictorKernel.scala` model + `PredictorModelBridgeSpec.scala` bridge (6 bridge tests, all GREEN); Stainless 0.9.9.3 with smt-z3 fallback (Z3 4.13.4); 35 VCs, 28 valid, 7 invalid (all "measure decreases" for wrapped tail calls — functions ARE terminating, each call processes a strictly smaller subtree), 0 unknown, 0.91s
- [x] Step 12 — concept-delta + inventory + module-graph update: created `openspec/concepts/optimizable-surface.md` + `openspec/concepts/predictor-state.md`; appended introduced concepts to `openspec/concept-inventory.md`; appended `adk4s-optimize` row to `openspec/project.md` module dependency graph + module descriptions
- [x] Step 13 — checkpoint: regenerate `tasks.md` checkboxes from `implementation-progress.md`; commit the spec; STOP for human validation before next spec (none — this is the only spec) — APPROVED, archiving
