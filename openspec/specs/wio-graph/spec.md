## Out of Scope

This change is a direct implementation of ADR-003 and is intentionally scoped to ADK4S `Graph` and its integration boundary with LLM4S orchestration.

`WIOGraph` and `WIONode` adapter factories are not part of this change.
